#!/usr/sbin/nft -f

table inet filter {
    chain input {
        type filter hook input priority 0;
        policy accept;
    }

    chain forward {
        type filter hook forward priority 0;
        policy drop;

        ct state established,related accept

        # Block HTTP and HTTPS from VM2 to external internet
        ip saddr 192.168.50.100 ip daddr != 192.168.50.0/24 tcp dport { 80, 443 } drop

        # Block ping (ICMP)
        ip saddr 192.168.50.100 ip daddr != 192.168.50.0/24 icmp type echo-request drop

        # Allow other traffic from VM2
        ip saddr 192.168.50.100 ip daddr != 192.168.50.0/24 accept

        # Allow everything else (from other sources)
        ip saddr != 192.168.50.100 accept
    }

    chain output {
        type filter hook output priority 0;
        policy accept;
    }
}
